//! Build-in moduels

// ? OS
// ? Path
// ? FS
// ? HTTP



// How to use Node,
// ! CLI in direory, type node to use the terminal like the nsole in a browser "Repli? redis google again"
//! to run something in Node (run the actual code in the terminal, ) type $ node filename.js -press Enter
<--\-- easy to way to type 'Hello World ' would be to console.log('hello world') and run Node --/->

// const amount = 12;

// if (amount > 10) {
//   console.log("Hellp big world");
// } else {
//   console.log("Hello small world");
// }
// Globals - No Window /console.log or Node run commands
//__dirname  path to current directory
//__filename  filename
//require  function to juse modules (Common JS)
//module  info about current module (file)
//process  info about env wher the programm is being excuted